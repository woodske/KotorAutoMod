<Window x:Class="KotorAutoMod.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KotorAutoMod"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10,10,10,4">
        <Button x:Name="SelectSwkotorFolderButton" Content="Select swkotor folder" HorizontalAlignment="Left" VerticalAlignment="Top" Click="SelectSwkotorFolderButton_Click" Margin="563,223,0,0"/>
        <ListBox x:Name="ModList" Margin="320,10,0,210"
   Width="450" Height="200" HorizontalAlignment="Left"
   ItemsSource="{Binding}" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="2">
                        <CheckBox x:Name="ModCheckBox" Content="{Binding ListName}" IsChecked="{Binding isChecked}" Width="{Binding ActualWidth, RelativeSource={RelativeSource FindAncestor, AncestorType=ListBoxItem}}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button x:Name="ApplyMods" Content="ApplyMods" HorizontalAlignment="Center" Margin="0,329,0,0" VerticalAlignment="Top" Click="ApplyMods_Click"/>
        <Button x:Name="CompressedModsFolderButton" Content="Select compressed mods folder" HorizontalAlignment="Center" Margin="0,267,0,0" VerticalAlignment="Top" Click="CompressedModsFolderButton_Click"/>
        <Button x:Name="TestModInstall" Content="TestModInstall" HorizontalAlignment="Left" Margin="84,230,0,0" VerticalAlignment="Top" Click="TestModInstall_Click"/>
        <ComboBox x:Name="ValidScreenResolutionsComboBox" ItemsSource="{Binding}" HorizontalAlignment="Left" Margin="32,283,0,0" VerticalAlignment="Top" Width="157" Text="Select Screen Resolution" IsEditable="True" IsReadOnly="True" SelectionChanged="ValidScreenResolutionsComboBox_SelectionChanged"/>
        <ComboBox x:Name="ValidAspectRatiosComboBox" ItemsSource="{Binding}" HorizontalAlignment="Center" Margin="0,234,0,0" VerticalAlignment="Top" Width="150" Text="Select Aspect Ratio" IsEditable="True" IsReadOnly="True" SelectionChanged="ValidAspectRatiosComboBox_SelectionChanged"/>
        <TextBlock x:Name="InstructionsTextBlock" HorizontalAlignment="Left" Margin="10,10,0,0" TextWrapping="Wrap" Text="{Binding}" VerticalAlignment="Top" Height="200" Width="287" Background="#FFB0BBAD"/>
    </Grid>
</Window>
