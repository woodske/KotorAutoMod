<Window x:Class="KotorAutoMod.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KotorAutoMod"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10,10,10,4">
        <Button x:Name="SelectSwkotorFolderButton" Content="Select swkotor folder" HorizontalAlignment="Center" VerticalAlignment="Center" Click="SelectSwkotorFolderButton_Click"/>
        <ListBox x:Name="ModList" Margin="171,0,0,220"
   Width="450" Height="200" HorizontalAlignment="Left"
   ItemsSource="{Binding}" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="2">
                        <CheckBox x:Name="ModCheckBox" Content="{Binding Name}" IsChecked="{Binding isChecked}" Width="{Binding ActualWidth, RelativeSource={RelativeSource FindAncestor, AncestorType=ListBoxItem}}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
